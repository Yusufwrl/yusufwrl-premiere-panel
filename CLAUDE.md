# PremiereExtension — Yusufwrl Altyazı Paneli

Adobe Premiere Pro **CEP uzantısı**. A1 mikrofon kanalındaki Türkçe konuşmayı Faster-Whisper-XXL (large-v3, GPU) ile otomatik **2-3 kelimelik** altyazıya çevirir. Sadece Windows.

## Yapı
- Panel arayüzü: `index.html` + `js/app.js` + `js/pipeline.js` — CEP (CSInterface.js, `--enable-nodejs --mixed-context`).
- Karakter sözlüğü: `js/sozluk.js` — özel isimleri düzeltir (Tofi/Moni/Dora/Mimi/Niko). İki katman: motora `--hotwords` ipucu + transkript sonrası kesin kelime düzeltmesi (zincirlenmiş Türkçe ekler dahil: "Tofilerden"). Liste `sozluk.json`'da (gitignore'lu), panelden düzenlenir.
- Senkron kartı: `js/kisiler.js` (Discord adı → karakter; **liste sırası = ses kanalı sırası**) + `js/hizala.js` (ses hizalama). Liste `kisiler.json`'da (gitignore'lu), panelden düzenlenir.
- Emoji aynalama: `js/pngayna.js` — kütüphanesiz PNG yatay çevirme (sol taraftaki karakterlerin resmi için). Premiere'e hiç dokunmaz, saf dosya işi; bu yüzden gerçek resimlerle repo içinde test edilebiliyor.

## Altyazı üretimi (js/app.js)
**TEK YOL: Premiere'in kendi altyazı kanalı.** Panel cue'ları SRT'ye yazar, `addCaptionsToTimeline` onu içe alıp caption track oluşturur. MOGRT yolu (renkli/animasyonlu, altyazı başına bir klip) **v1.8.0'da tamamen kaldırıldı** — 20 dakikalık videoda ~1000 klip üretip Premiere'i kilitliyordu. Görünüm (yazı tipi, renk, kontur, konum) Premiere'de bir kez ayarlanır: altyazıya tıkla → Essential Graphics → **Track Style → Create Style**.
- Bunun bedeli: **animasyon yok** (caption klipleri efekt/keyframe kabul etmez; `Pop In` gibi preset'ler ancak `Graphics and Titles > Upgrade Caption to Graphic` ile klibe çevrildikten sonra uygulanabilir — o da altyazı başına bir klip demek).
- **Karaktere göre renk MÜMKÜN — kanal başına ayrı altyazı kanalı ile.** Stil kliplerin değil **TRACK'in** ayarıdır (Caption Track Settings > Style), yani bir track = bir stil. Ama Premiere birden çok caption track destekliyor (Text panelinin kendi ifadesi: "Multiple caption tracks enabled") ve her birinin kendi stili olur. Bu yüzden `placeCaptions` **"Herkes" kaynağında her ses kanalını AYRI caption track'e** yazar (A1 → 1., sonra `aktifKanallar()` sırasıyla). **Çağrı sırası = track sırası**; hangi kanalın kaçıncı track'e gittiği log'a ve sonuç mesajına yazılır, yoksa kullanıcı hangi track'e hangi stili vereceğini bilemez. Tek kaynak seçiliyken (A1/A2/A3/A1+A2) tek grup → tek track.
  **Doğrulandı** (kullanıcı, gerçek projede, 5 Ağustos 2026): kanal başına ayrı track oluşuyor ve çalışıyor.
  **Stili panel ATAYAMAZ — ÜÇ YÜZEY DE ÖLÇÜLDÜ, BİR DAHA DENEME.** (Premiere **26.3.0**, 6 Ağustos 2026, kullanıcının makinesinde `captionStilTani` ile `reflect` dökümü alınarak.)
  1. **Normal DOM sequence:** 15 özellik / 42 metot. Caption'la ilgili **tek** şey `createCaptionTrack` — bir *yaratma* metodu. Geri okuma/listeleme/stil verme metodu yok. `captionTracks` · `captions` · `captionTrack` · `getCaptionTrackCount` · `getCaptionTrackAt` · `setCaptionTrackStyle` · `captionStyle` → hepsi `undefined`.
  2. **`createCaptionTrack`'in 3./4. parametresi:** stili almıyor ve **hata da vermiyor** — sessizce yok sayıyor (bu yüzden "denedim, olmuş görünüyor" tuzağı var).
  3. **QE DOM sequence:** 27 özellik / 63 metot. `getVideoTrackAt` · `getAudioTrackAt` · `addTracks` · `removeTracks` var ama **caption geçen tek bir metot bile yok** — Premiere'in kendi iç arayüzü caption track'leri hiç tanımıyor.
  **Sonuç:** `createCaptionTrack` "yaz ve unut" bir çağrı; track oluştuktan sonra ona erişim yok. Dördüncü bir yüzey yok — tek teorik yol UXP'ye taşınmak. Ölçüm fonksiyonu (`host.jsx` → `captionStilTani`) arayüzden kaldırıldı ama **duruyor**: yeni bir Premiere sürümünde tekrar ölçmek için DevTools'tan `new CSInterface().evalScript("captionStilTani()", console.log)`. Panel bu yüzden yalnızca **hangi kanala hangi stilin verileceğini** sonuç mesajına yazar (`C1 (sen) → Pink Text · C2 (Dora) → Siyah Text`); uygulamayı kullanıcı Premiere'de Track Style'dan elle yapar. `#trackStilBox`'taki dinamik stil seçicileri bu talimatı üretmek için var. ⚠ Export'ta `Burn Captions Into Video`'nun **bütün** track'leri gömüp gömmediği ayrıca ölçülmedi — ilk gerçek dışa aktarımda kontrol edilmeli.
- Kaldırılanlar: stil seçici, "Konuşmacıya Göre" ve "Renk Değiştir" modları, katman/lane mantığı, `videoKanaliYeterMi`, `stackShifter`, Shorts konum kaydırıcıları, host'taki `addMultiStyleSubtitles`/`addLanedSubtitles`/`addStyledSubtitles`/`recolorSelected` ve MOGRT yardımcıları (~900 satır).
- **Altyazı artık video kanalı TÜKETMİYOR** — "en alt video kanalı silinir" sınıfı hataların tamamı ortadan kalktı.
- **Kaynak Ses** seçenekleri: **A1 · A2 · Herkes** (A3 ve A1+A2 kaldırıldı — A3 kadroya göre anlam değiştiriyor ve "Herkes" onu kapsıyor; A1+A2'nin tek karışık kanalı ise "Herkes"in kanal ayrımı yanında hep daha kötüydü). Kayıtlı eski seçim (`yw.track` = "2"/"mix") açılışta düğmeyi bulamayınca **sessizce A1'e düşmez** — `restoreSegs` değeri temizler ve kullanıcıya söyler; sessiz düşüşün bedeli 20-30 dakikalık boşa GPU işi. `prepAudioMix` silindi, `pipeline.mixWavs` KALDI (AutoCut kullanıyor).
- **HER KARAKTERE AYRI ALTYAZI KANALI:** C1 = videoyu çeken (A1) · sonra yazıya dökülen her ses kanalı kendi track'ine, `aktifKanallar()` sırasıyla. **Bu karar iki kez döndü, tarihçesini oku ve üçüncü kez döndürmeden önce buraya bak:** (1) kanal başına ayrı track yapıldı ve gerçek projede doğrulandı → (2) her videoda track sayısı kadar elle stil vermek gerektiği için kullanıcı sabit iki kanala (C1 = sen, C2 = arkadaşlar birleşik) indirdi → (3) karaktere göre renk istendiği için geri alındı, elle stil verme bedeli **bilerek** kabul edildi.
  - **STİL SEÇİCİLERİ KALDIRILDI (kullanıcı isteği, 6 Ağustos 2026) — geri ekleme.** Kanal başına bir "Track Style" seçici vardı (`#trackStilBox` + `trackStilDoldur()` + `captionStilleriTara()` + `stilSatir/stilSecDoldur/stilDegeri`, `kanalStil.*` / `trackStil.tek` anahtarları). **Stili hiçbir zaman UYGULAMIYORDU** — panel caption track'e erişemiyor (üç API yüzeyi de ölçüldü, aşağıdaki nota bak); tek işi sonuç mesajına "C1 (sen) → Pink Text" diye bir talimat yazdırmaktı. Bedeli: 7 kanallı kadroda ekranı 7 satır boş seçici dolduruyordu. Şimdi sonuç mesajı yalnız **hangi karakterin kaçıncı altyazı kanalına** yazıldığını söylüyor (`C1 sen · C2 Dora`) ve birden çok track varsa "Stilleri Premiere'de ver: altyazıya tıkla → Track Style" diye hatırlatıyor.
    ⚠ host.jsx'teki `captionStilleriJSON` ve `addCaptionsToTimeline`'ın `".stil"` dosyası okuması **duruyor ama artık ÇAĞRILMIYOR** — panel o dosyayı hiç yazmadığı için sessizce devre dışı. Çalışan altyazı yolunu kurcalamamak için silinmedi.
  - **Kullanıcının Track Style'ları PANELDE DEĞİL, Premiere PROJESİNDE durur** (`.prproj` içinde; "Save to Local styles" denirse `Belgeler\Adobe\Common\Assets\Text Styles`). Panel güncellemesi/yeniden kurulumu onlara **dokunmaz** — kullanıcı bunu sordu, cevap bu.
  - ⚠ **"Ayrı track'te oldukları için birbirini ezmiyor" İDDİASI ÇÜRÜDÜ (7 Ağustos 2026, kullanıcı ekran görüntüsü + gerçek oturum ölçümü).** Doğru olan tek şey METİN KAYBI idi (aynı track'e düşen cue'yu Premiere yutuyordu); GÖRSEL eksende yanlış — Premiere caption track'lerinin hepsini **aynı anda ve aynı yerde** çiziyor, yani aynı anda konuşanların yazısı ekranda üst üste biniyor. Ölçüldü (`oturum_Video Sequence.json`, 233 cue / 5 grup): kanal içi çakışma **0** (`cakismaGider` işini yapıyor), kanallar **ARASI 35 çift / 12.41 sn**. Çözüm aşağıda.
  - **KANALLAR ARASI ÇAKIŞMA — `pipeline.kanallarArasiCakisma`, `placeCaptions` içinde çağrılıyor.** `cakismaGider` grup BAŞINA çağrıldığı için kanalları tek zaman ekseninde gören kod hiç yoktu. **Politika: KIRP → (kutu açıksa) GİZLE → İTME ASLA.** Kırpma yalnız bitişi kısaltır, başlangıca dokunmaz — **senkron bozulmaz** ve tek başına çakışmanın %75'ini çözer (35 → 9), çünkü cue bitişlerinin çoğu konuşmanın bitişi değil `buildCues`'un `minDur = max(0.8, harf/17)` ile doldurduğu **yapay kuyruk**. Cue **silinmez**, `gizliCakisma` diye işaretlenir ve yalnız o yerleştirmede SRT'ye yazılmaz. Ölçülen sonuç: kırpma+gizleme **35 → 0 çift**, bedeli 12 cue (%5).
    - **İTME DALI BURAYA EKLENMEZ** — sonraki cue'yu ileri itmek kullanıcının şikâyet ettiği kaymanın ta kendisi. İtmemek dizi zaman sırasını da bozmuyor, yani `cuesToSrt` öncesi yeniden sıralama gerekmiyor.
    - **GİZLEME CUE BAZINDA, CÜMLE BAZINDA DEĞİL — ikisi de ölçüldü.** Cümle bütünlüğünü korumak (aynı `cumleId`'nin tamamını gizlemek) sezgisel olarak doğru ama **4-5 kat pahalı**: 49-70 cue düşüyor, cue bazlıda 9-16. Sebep: kelime tavanı 2 olduğu için bir cümle 4-5 cue ve her çakışma koca bir repliği siliyor. Bedeli bilinçli: 7-10 cümlede 2 kelimelik boşluk kalıyor, ses zaten duyuluyor.
    - **KURBAN = GRUP SIRASI, başka hiçbir şey. "Kısa cümle/cue kaybetsin" kuralları DENENDİ ve gerçek veride TERS çalıştı:** kullanıcı (C1) kısa cümlelerle çok, arkadaşlar uzun cümlelerle az konuşuyor → "ağırlık" kuralında 54 gizlemenin **47'si kullanıcının kendi sesi** oldu. Grup sırasında C1 hiç kaybetmiyor (Sage 3 · Mimi 4 · Moni 5). **Geri ekleme.**
    - **TABAN 0.25** (= `buildCues` MIN_GORUNUR). Bilerek düşük: takas "kısa görünen altyazı mı, HİÇ görünmeyen mi" ve kullanıcı üst üste binmesin dedi, altyazı kaybolsun demedi. Ölçüldü: 0.20 → 9 gizleme · 0.25 → 12 · 0.30 → 13 · 0.40 → 16.
    - **Kutu: "Asla üst üste gelmesin"** (`#chkCakisma`, `cakismaGizle()`, varsayılan AÇIK). Kapalıyken yalnız kırpma olur, kalan çakışma sayılır. **Gizlenen sayısı eşiği (max 20 veya %3) aşarsa `placeCaptions` YERLEŞTİRMEDEN ÖNCE onay sorar** — bedel moda göre 5 kat değişiyor ve sarı bir satır büyük kayıp için yeterli değil.
  - ⚠ **`placeCaptions` artık cue nesnelerinin KOPYASI üzerinde çalışıyor** (vurucu bloğundan SONRA, genel `for...in` sığ kopya — alan listesi TUTMA, yeni alanlar sessizce düşer). Eskiden `isler[].cues` doğrudan `state.a1Cues` / `ch.cues` nesneleriydi (`c.slice()` yalnız diziyi kopyalıyor): kırpma state'e işliyor, `saveSession` bozuk zamanları diske yazıyor ve ikinci basışta cue'lar ikinci kez kırpılıyordu. Bugüne kadar görünmemesinin tek sebebi kanal içi gidericinin bu veride **hiçbir şey değiştirmemesiydi** (0 kırpma / 0 itme — tam bir no-op).
  - ⚠ **ÜST ÜSTE YAZININ ZAMANLAMADAN BAĞIMSIZ İKİNCİ SEBEBİ: her basış YENİ track yaratıyor.** `host.jsx addCaptionsToTimeline` koşulsuz `seq.createCaptionTrack` çağırıyor; eski caption track'leri silen/sayan/yeniden kullanan kod YOK (panel caption track'e hiç erişemiyor). İkinci basış 5 track DAHA yaratır ve her altyazı ekranda **iki kez** çizilir. Panel sayamadığı için tek çare sormak: `capBasildi_<sekans>` bayrağı + onay. **Kullanıcı yamadan sonra hâlâ üst üste görüyorsa ÖNCE bunu sor.**
  - `cakismaGider` **ÇOK TURLU (4) ve her turdan sonra diziyi YENİDEN SIRALAR** — itme cue'yu zamanda öne alıyor ama dizideki yerini değiştirmiyordu ve `cuesToSrt` diziyi olduğu sırayla yazdığı için SRT'de **zamanı geriye giden satırlar** oluşuyordu (ölçüldü: 800 cue'da 45 satır; Premiere böyle bir SRT'yi reddedebilir). Sıralama aynı zamanda bir sonraki turun yeni komşu çiftleri görmesini sağlıyor: ölçülen kalan çakışma 1823 → 920 → 669 → 630 → **624**, 4. turdan sonra kazanç sıfır. **Çakışma tam çözülmez** — kalan sayı log'a yazılır (`[cakisma] N altyazi HALA ust uste kaliyor`), çünkü metin silmektense üst üste bırakmak yeğdir.
- **Kelime tavanı HER ZAMAN 2** (Shorts'tan bağımsız, `trOpts` içinde kodda zorlanır). `config.json`'a GÜVENME: `updater.js configBirlestir` kullanıcının mevcut `maxWordsPerCue` değerini koruyor, dosyayı değiştirmek kurulu panele hiç ulaşmaz. Tavanı düşürmek tek başına hizalamayı KÖTÜLEŞTİRİYOR (ölçüldü: sessizde başlayan cue 90 → 138) — `sesleHizala` bu yüzden aynı pakette onarıldı: cue kaydırılırken **bitişi de birlikte** taşınıyor, "okuma süresi kalmıyorsa dokunma" freni kalktı.
- **"Tofi Moni video modu"** (`js/vurucu.js`): ilk 60 sn herkesin konuşması tam (cümle bitene kadar aşabilir), sonrasında ~20 sn'de bir yalnız en vurucu cümle. Seçimi **Claude API** yapıyor — panelin internete çıktığı TEK yer, kutu kapalıyken hiç istek gitmez. Yerel sinyaller (ses zirvesi, ünlem, konuşma hızı) kullanıcının gerçek kaydında ölçüldü ve ZAYIF çıktı: ünlemli/ünlemsiz tepe farkı 0.1 dB, pencerelerin %39'unda hiç ünlem yok, konuşma hızı ters işaretli. Cue'lar **silinmez, işaretlenir** (`vurucuGoster`) — mod kapatılınca `VUR.temizle` ile işaret kalkar, 25 dakikalık GPU işi tekrar edilmez. Anahtar: `anthropic-key.txt` (kullanıcı dosyası, **beş listede** kayıtlı).
- Cue'larda **`cumleId`** var (`<oturum jetonu>-<kaynakNo>:<segment>`): vurucu mod bir cümlenin BÜTÜN cue'larını birlikte göstermek için buna güveniyor. Jeton şart — sayaç panel yeniden açılınca sıfırlanıyor ama işaretsiz kanalın eski cue'ları korunuyor, jetonsuz kimlikler çakışırdı.
- **Karakter İsimleri kartı → Ayarlar görünümü** (`viewAyarlar`). Kart SİLİNMEDİ, taşındı; id'leri (`dictText`/`dictSave`/`dictReset`/`dictBadge`/`dictStatus`) aynı kaldığı için sözlük kodunda tek satır değişmedi. Silmenin bedeli olurdu: `dictApply`, üretilmiş altyazıyı yeniden GPU'ya sokmadan düzelten tek yol.
- **AutoCut sonrası bayatlık freni:** kesim başarılı olunca elde cue varsa `state.cuesStale` kurulur; `placeCaptions` onay sorar. Kesim timeline'ı kısalttığı için eski cue'lar **dakikalarca** kayardı ve panel hiç fark etmiyordu (hizalama tartışması 0.3 sn'likken bu senaryo dakikalarca). "Herkes" eski "Konuşmacıya Göre"nin yerini alır: klip içeren her kanal AYRI yazıya dökülür (üst üste konuşmalar karışmaz, doğruluk yüksek) ve `placeCaptions` içinde kendi zaman sırasına dizilip **kendi caption track'ine** yazılır.
- **Shorts** kutusu duruyor ama artık yalnız **kelime bölmesini** değiştiriyor: `maxWords 2` + `maxChars 16` (uzunluğa göre 1, en fazla 2 kelime). Konum/boyut Premiere'in altyazı stilinden ayarlanır.
- **AYNI CÜMLEDE KELİME ARASI BOŞLUK KAPATILIR — `pipeline.cumleBirlestir`, `placeCaptions`'ta SRT yazılmadan HEMEN ÖNCE** (kullanıcı isteği, 8 Ağustos 2026: "kelime aralarında boşluk oluyo, garip duruyo — tek cümleyse birleşik olmalı"). Sebep ölçüldü: `buildCues` her cue'nun bitişini `sonraki.start - MIN_GAP`e **dayıyor** ve `MIN_GAP = 0.08`; kelime tavanı 2 olduğu için hızlı konuşmada cue'ların çoğu bu tavana çarpıyor, yani neredeyse HER cue sınırında 2 karelik boşluk kalıp yazı yanıp sönüyor. Çözüm: aynı `cumleId`'li ardışık cue'larda `end = sonraki.start`. **Cümleler ARASINDA boşluk korunur** — orası bilgi taşıyor ve kullanıcı da "tek cümleyse" dedi.
  - **ÖLÇÜLDÜ (gerçek oturum, 172 cue / 5 grup, gerçek üretim sırası simüle edilerek):** 0-0.15 sn boşluk **122 → 3**; metin kaybı **0 cue**; yeni çakışma **0** (ne kanal içi ne kanallar arası); gizlenen ve kırpılan sayıları **değişmiyor** (4 ve 13).
  - **SIRA BİLEREK EN SONDA — üç sebebi var.** (1) `kanallarArasiCakisma`'dan SONRA çalışır, yani köprü yeni çakışma doğurup KIRPMA/GİZLEME tetikleyemez; fonksiyon kendi içinde diğer kanalları kontrol edip gerekirse köprüyü hiç kurmuyor (yanıp sönmeyi düzeltmek için altyazı kaybetmek yanlış takas). (2) Gizlenenler listeden düşürüldükten sonra çalışır, yani köprü gerçekten ekrana çıkacak komşuya kurulur. (3) Cue KOPYASI üzerinde — panelin listesi, oturum dosyası ve emoji süreleri etkilenmez.
  - **ÜÇ VARSAYIM ÖLÇÜLDÜ, ÜÇÜ DE TUTUYOR:** `cuesToSrt` damgayı `Math.round(t*1000)` ile üretiyor, `end` ile `next.start` aynı sayıdan geldiği için aynı ms dizgisi çıkıyor (ters/çakışan satır yok) · `cakismaGider` testi `onc.end <= snr.start` → sıfır boşluk çakışma DEĞİL · `kanallarArasiCakisma` testi `B.start >= A.end` → o da değil. Yani iki giderici de köprüyü geri açmıyor.
  - `start`'a **ASLA** dokunulmaz (senkron bu projenin en pahalı şeyi) · `cumleId` yoksa köprü YOK (cümle sınırını uydurmaktansa boşluk kalsın) · `MAX_KOPRU = 0.15` sn, **büyütme**: bunun üstü yapay değil GERÇEK duraklamadır ve altyazının orada kaybolması doğrudur.
  - **FUZZ ÖLÇÜMÜ (3000 rastgele senaryo, 2-5 grup, 8-32 cue/grup, 3885 köprü): yeni doğan kanallar arası çakışma = 0.** Fonksiyon ayrıca kendi içinde bir **doğrulama turu** koşuyor (`sayac.kalanKanallarArasi`) ve kalan çakışma varsa log'a yazıyor — "köprü çakışma doğurmaz" bir iddia, ölçülmeden yazılmamalı.
  - ⚠ `baskaKanalVar` kontrolü cue nesnesinin **REFERANSINI** tutar, start/end kopyasını değil (aynı turda kurulan köprüler de görünsün). Denetimde "bayat anlık görüntü çakışma sızdırır" diye bildirildi; **fuzz'da iki sürüm 3000 senaryonun hiçbirinde farklı davranmadı**, yani o yol pratikte ulaşılamıyor. Düzeltme yine de duruyor (canlı okumak bedava ve daha doğru) ama gerçek bir hatayı düzelttiği İDDİA EDİLMEMELİ.
- **Altyazı sese hizalanır** (`sesleHizala`, pipeline.js). Motorun kelime damgası konuşma DUYULMADAN önceye düşebiliyor; suçlu panel değil, kaynak motorun damgası. Cue başında ses yoksa cue **ileri** kaydırılır — asla geriye.
  - ⚠ **ESKİ "185 → 36" ÖLÇÜMÜ ÇÜRÜDÜ.** Kullanıcının gerçek A1 kaydıyla yeniden ölçüldü (`A1.wav` + `A1.json`, 1059 cue): eski kod sessizde başlayan **236 cue'nun yalnız 21'ini** düzeltiyordu (236 → 215); geri kalanı "tavana çarptı" diye atlanıyordu.
  - **KÖK SEBEP ARİTMETİK ÖZDEŞLİKTİ:** `buildCues` her cue'nun bitişini `sonraki.start - MIN_GAP`'e **dayıyor** ve `MIN_GAP` ile `sesleHizala`'nın `HIZ_GAP`'i **aynı sayı (0.08)** olduğu için `(sonraki.start - HIZ_GAP) - cue.end` tavanı **tam sıfır** çıkıyordu. Cue'ların **%78'i** yapısal olarak dokunulamazdı. Bu bir ayar meselesi değildi.
  - **ÇÖZÜM (uygulandı): tek formül, iki tavan.** `sonKaydir` = bitişin gidebileceği kadarı (rijit tavanla sınırlı), `basKaydir` = başlangıcın gidebileceği kadarı (cue'ya `MIN_GOR = 0.40` kalmak şartıyla). Yer varsa cue **rijit** kayar (eski davranışla birebir); yer yoksa **yalnız başlangıç** ilerler, bitiş yerinde kalır — kısalan kısım tanım gereği **sessizlikti**. **Ölçüldü: 236 → 140**, ort. gecikme 0.271 → **0.138 sn**, gerileyen cue **0**, geriye kaydırılan **0**, `<0.30 sn` cue sayısı **değişmiyor** (165 → 165).
  - **`ARA_PENCERE` (1.20) ile `MAX_KAYDIR` (0.60) AYRI ŞEYLER — birleştirme.** Eskiden ikisi de 0.60'tı: 0.60 sn'den geç başlayan konuşmada cue "pencere dışı" sayılıp **hiç** düzeltilmiyordu. Artık konuşma 1.20 sn'ye kadar aranıyor ama cue yine en fazla 0.60 kayıyor — yer değiştirme sınırı büyümediği için "yanlış onset'e yapışma" riski de büyümüyor. Tavana dayananlar log'a **ayrı** yazılıyor ("hâlâ erken olabilir"), çünkü onlar tam düzelmiyor.
  - `opts.sesHizala === false` ile kapatılır. Yan not: rijit dal cue'yu ileri **uzatabildiği** için yeni bir kanallar arası çakışma doğurabilir — `kanallarArasiCakisma` bu yüzden `sesleHizala`'dan SONRA çalışmak zorunda.
- **Diarizasyon (AI konuşmacı tahmini) KALDIRILDI.** Ölçüldü: tek kişilik kayıtta pyannote_v3.1 4, reverb_v2 3, ücretli AssemblyAI 4 konuşmacı buldu (doğrusu 1). Eski diarizasyonlu oturumlar geri yüklenirken A1+A2 tek listeye birleştirilir.
- Üretim sonunda oturum `%ENGINE%\work\oturum_<sekans>.json`'a yazılır; panel açılışında geri yükleme teklif edilir.

## Senkron kartı (Craig kayıtları)
Discord'da Craig bot'un kişi başına ayrı aldığı kayıtları (`1-yusufwrl.m4a`) otomatik hizalar ve doğru kanala koyar. Diarizasyonun yerini alır — ayrım %100 doğru.
- **Kanal kuralı:** A1 = videoyu çeken (OBS mikrofonu, dokunulmaz) · A2 = Tofi/Moni'den diğeri · sonra `kisiler.json` **sırasıyla** kalan karakterler · **en son oyun sesi**. Videoda olmayan karakter kanal harcamaz, alttakiler yukarı kayar (Sage+Niko yoksa oyun sesi A5). Çekenin Craig kaydı **timeline'a konmaz**, yalnızca hizalama referansıdır.
- **Oyun sesini panel TAŞIMAZ** — kullanıcı klibi Premiere'de **elle** en alt kanala taşır; panel yerleşim tablosunda "buraya SEN taşı" der ve o kanal boşsa hem satırda hem onay ekranında uyarır. "Oyun sesi şu an hangi kanalda?" seçici kaldırıldı. **Sebep:** Premiere'de klip taşıma API'si yok; tek yol `overwriteClip` ve o **project item'dan** yerleştirdiği için çoklu-akışlı OBS kaydında (A1/A2/A3 = aynı dosyanın 1./2./3. akışı) hedefe oyun sesi değil **mikrofon** düşüyor. `kanalTasi` bunu tespit edip taşımayı reddediyordu — yani özellik kullanıcının kaydında **hiç çalışmıyordu**, sadece "Oyun sesi taşınamadı" hatası üretiyordu. Fonksiyon `host.jsx`'te **duruyor ama çağrılmıyor**: tek akışlı kayıtta (oyun sesi ayrı dosyaya kaydedilirse) güvenle çalışır, gerekirse yeniden bağlanır.
- Onay ekranından **hemen önce** `snkKanalSayisiOku()` çağrılır — kullanıcı oyun sesini panel hizalama yaparken taşımış olabilir; bayat veriyle sorulursa "üzerine yazılacak" uyarısı yanlış çıkar.
- **Timeline renk etiketi YOK** (kullanıcı iptal etti). `setColorLabel` çağrısı kaldırıldı, plan dosyası biçimi `yol|kanal|başlangıç|ad`.
- Hizalama enerji zarfı çapraz korelasyonu (`js/hizala.js`): kaba 200 ms → ince 20 ms, Pearson r. **Güvenilirlik sinyali korelasyon değil, medyan tutarlılığıdır** — tek başına r doğru eşleşmeyi yanlıştan ayıramıyor (ölçüldü). `tutarlilikKontrol` diğerlerinden sapan kaymayı işaretler.
- Her karakterin her videoda olması gerekmez — eksik karakter normaldir, hata değil. Aynı kişiye iki dosya düşerse (Craig `_2` eki) ilki ana kayıt, kalanlar "(2. kayıt)" etiketiyle ayrı kanala gider.
- Premiere'de **ses kanalı ekleme API'si yok** — kanal yetmezse panel uyarır, kullanıcı elle ekler. Yerleştirme yalnızca `audioTracks[i].overwriteClip(item, saniye)` ile yapılır; sekans düzeyindeki 4 parametreli biçim hedef kanalı garanti etmez ve A1'i ezebilir.
- Host katmanı: `jsx/host.jsx` — **ExtendScript (ES3)**: `let`/`const`/arrow/optional-chaining **YOK**. Premiere'i bu dosya sürer.
- Manifest: `CSXS/manifest.xml` — BundleId `com.yusufwrl.premierepanel`, PPRO 14+, CSXS 9.
- Motor harici: `%ENGINE%\Faster-Whisper-XXL` (varsayılan `C:\Users\yusuf\YusufwrlEngine`). Repoya dahil değil, gitignore'lu (~3GB).

## AutoCut (sessizlik kesme)
Konuşma olmayan boşlukları bulup timeline'dan ripple-delete eder. **Boşluk = işaretli kanalların HİÇBİRİNDE konuşma yok** demek.
- **Kanallar sabit DEĞİL, tahmin de yapılmaz.** Panel klip içeren tüm ses kanallarını listeler; varsayılan **hepsi işaretli**. Asimetrik risk: bir kanalı yanlışlıkla dışarıda bırakmak o kişinin konuşmasını sildirir, içeride bırakmak yalnızca "boşluk bulunamadı" dedirtir. Seçim `yw.acCh2_<idx>`'te hatırlanır — kanal numaralarının ANLAMI değiştiği için eski `acCh` anahtarı bilerek terk edildi.
- Eskiden A1+A2 **sabitti**; Senkron kartı arkadaşları A4+'ya koyduğu için onların sesi analize hiç girmiyor, yalnız A4'te biri konuşurken o bölüm boşluk sayılıp **kesiliyordu**. Yeni kanal eklerken bu tuzağı hatırla.
- Bir kanalın sesi hazırlanamazsa analiz düşmez ama **sessizce de geçilmez** — log'a "ATLANDI" düşer, çünkü o kanaldaki konuşma kesilir.
- Kesim maliyeti boşluk SAYISIYLA büyür: "En kısa boşluk" 0.1 sn binlerce kesim üretip saatlerce sürebilir; 0.3 sn neredeyse aynı kazancı çok daha hızlı verir.

## Sürüm öncesi denetim — `node testler\tumtest.js`
**Her sürümden önce çalıştır.** Premiere GEREKMEZ; saf dosya + saf hesap. 41 kontrol: sürüm üç dosyada senkron mu · emoji paketi kaynakla aynı mı (`emoji-esitle.ps1` unutuldu mu) · ad çözümleme ve havuzlar · PNG aynalama + yan chunk'lar (sRGB/pHYs) · altyazı köprüsü uç durumları + 500 turluk fuzz · emoji sıra numaralandırması (emoji yanlış kişiye gitmesin) · host.jsx ES3 taraması · HTML↔app.js id bağlantıları · dağıtım zinciri.
- **Yeni bir hata bulunduğunda buraya bir test EKLE.** Buradaki her kontrol gerçekten yaşanmış bir hatadan doğdu; hiçbiri teorik değil.
- `testler` klasörü `$PanelIgnore`'da — panele GİTMEZ. (Bu eksiği testin kendisi yakaladı: "hiçbir listede olmayan öğe: testler".)
- **Kapsam dışı (bilerek):** Premiere API'si gerektiren her şey — klip yerleştirme, keyframe, caption track. Onlar ancak kullanıcının makinesinde ölçülüyor.

## Emoji (tepki resmi) — `js/emoji.js` · app.js `emojiEkle` · host.jsx `emojiYerlestir`/`emojiTemizle`/`emojiKanallariJSON`
- **KENDİ EKRANINDA (`viewEmoji`, 9 Ağustos 2026)** — Altyazı kartından çıkarıldı. Sebep yer darlığı değil: emoji hangi sesin kime ait olduğuna bağlı ve o bilgi Altyazı ekranındaki isim kutularına gömülüydü; yanlış eşleşme ancak videoyu izlerken fark ediliyordu.
  - Ekranın asıl işi **"Bu videoda kim hangi kanalda?"** listesi: her ses kanalının karşısında bir karakter seçici (otomatik / karakterler / emoji yok) ve altında tek satırlık özet (`A1→Tofi · A2→Moni · A3→Mimi`). Basmadan önce görülüyor.
  - Varsayılan hâlâ **kanal adından** türetiliyor; liste onu **ezebilir** ve seçim `emojiCumleleriTopla`'da ad eşleşmesinin ÖNÜNDE uygulanıyor (yoksa ekran süs olurdu).
  - ⚠ **Anahtar SEKANSA ÖZEL** (`emoji.kanalKar.<sekansKimlik>.<idx>`): kanal numarasının anlamı kadroya göre değişiyor (A4 bir videoda Sage, ötekinde Niko). Kalıcı saklamak sonraki videoda sessizce yanlış yüzü koyardı — aynı ders AutoCut'ta `acCh → acCh2_` göçüyle, emoji eşleşmesinde ADLA saklamayla alınmıştı.
  - Seçilen karakter klasörden silinmişse seçim geçersiz sayılıp normal ad yoluna dönülür (sessiz düşüş yok).
- ⚠ **EMOJİ YANLIŞ KİŞİYE GİDİYORDU — PARÇA NUMARALANDIRMASI** (ParsMazi, 9 Ağustos 2026; v1.9.15'te düzeltildi). `duygulariSec` cümleleri 250'lik parçalar hâlinde gönderiyor ve satırları **global** sıra numarasıyla yazıyordu (`"251 [moni] …"`). Model 250 satırlık listeyi 1'den numaraladığında (bir listeyi numaralamanın en doğal yolu) cevabındaki "1" panelde **1. cümleye** düşüyordu — başka parçanın, başka konuşmacının cümlesine. **Panelde hiçbir doğrulama yoktu.**
  **ÖLÇÜLDÜ** (400 cümle: 250 Tofi + 150 Moni, 2 parça): 150 cümle iki kez seçildi, 150 cümle hiç seçilmedi; Moni'nin 150 cümlesinin tamamı emojisiz kaldı ve onun için seçilen duygular Tofi'ye yazıldı. **Kısa videoda (tek parça) HİÇ görünmüyor** — geliştiricinin test oturumu tek parçaydı, o yüzden aylarca fark edilmedi.
  **Düzeltme:** her parça kendi içinde `1..N` numaralanıyor, cevap `dilim[numara-1]` ile geri eşlenip GERÇEK sıra yazılıyor, aralık dışı numara atılıp SAYILIYOR. Cevap bozuksa emoji *az* çıkar ama **yanlış kişiye gitmez.** Testi `testler\tumtest.js` içinde.
Konuşanın duygusuna göre **sağ alt köşeye** karakterin tepki resmi (PNG) koyar. Varlıklar: `Youtube\Edit\Emoji` → `<Duygu> <Karakter>.png` (5 duygu × 4 karakter). Duygu/karakter kümesi **klasörden türetilir**, koda gömülmez.
- **ÖLÇÜLMÜŞ Premiere değerleri** (26.3.0): Position **NORMALİZE 0..1** (piksel DEĞİL) · still varsayılanı **5 sn**, `Time.ticks` ile ezilir · Scale yüzde · PNG boyutları farklı (Tofi 2000², diğerleri 1000²) → **ölçek dosya başına** hesaplanır.
- **Boyut KULLANICI ONAYLI, DOKUNMA:** `EMOJI_ORAN = 0.574` (kare yüksekliğinin %57'si). Kullanıcı elle ayarlayıp ekran görüntüsüyle onayladı; ilk denemedeki %22 + 54 px "çok küçük ve içeride" diye reddedildi.
- **Kenar boşluğu İKİ EKSENDE AYRI:** `EMOJI_BOSLUK_X = 0.005` (sağda ≈5 px) · **`EMOJI_BOSLUK_Y = 0`** — emoji alt kenara DAYANIR (kullanıcı isteği: "aşağıda o küçücük yer kapansın"). 1920×1080'de sonuç: 620 px emoji, merkez `0.836 / 0.713`, alt boşluk **0 px**.
- ⚠ **PNG'LERİN KENDİ KENAR PAYI KODLA DÜZELTİLEMEZ — ÖLÇÜLDÜ (alfa bounding box, 30 dosya).**
  - **Çizim SOL kenara dayalı (kesik) 4 dosya:** `Ağlayan Tofi` · `Düşünceli Tofi` · `Mutlu Tofi` · `Şaşırmış Tofi` (sol şeffaf pay %0.0). Kullanıcı "solda dümdüz çizgi gibi kesiyor" diye bunu bildirdi; kesilen piksel dosyada yok, panel geri getiremez — **resmi yeniden kaydetmek gerekir.**
  - **ALT şeffaf payı olan dosyalar** (`EMOJI_BOSLUK_Y = 0` olsa bile boşluk bırakırlar): `Çok Mutlu / Havalı / Mızmızlanan / Heyecanlı Tofi 2` %10.9 · `Şaşırmış Moni` %12.2 · `Şaşırmış Mimi` %3.1. Diğerlerinde alt pay %0, yani tam oturuyorlar.
  - Ölçüm yöntemi: PNG'yi `zlib.inflateSync` + filtre çözümüyle aç, alfa > 8 olan piksellerin sınırlarını bul. Panele KONMADI (30 dosyanın çoğu 2000², "Tara" düğmesini saniyelerce bekletirdi); gerekirse ayrı bir denetim düğmesi olarak eklenebilir.
- **TARAF: Tofi/Moni SAĞDA, konuklar SOLDA + resim yatay AYNALANIR** (kullanıcı isteği, 8 Ağustos 2026). Bu karar **iki kez döndü**, üçüncü kez döndürmeden önce tarihçeyi oku: (1) "sen sağda, arkadaşlar solda" → (2) 7 Ağustos'ta "hepsi sağda" denip `emojiKonum`'dan `sag` parametresi tümden kaldırıldı ("dal yoksa yanlış taraf da geçilemez") → (3) 8 Ağustos'ta iki taraf geri istendi. Fark: tarafı artık **çağıran seçmiyor**, `emojiSagMi(karakterKey, a1Mi)` belirliyor. Sol x, sağ x'in kare merkezine göre tam aynası (`solX = 1 - sağX`), çünkü iki kenar boşluğu da aynı `bosX`'ten geliyor (1920×1080'de 0.836 / 0.164).
  - **İKİ KURAL VAR, İKİSİ DE ŞART.** (a) `EMOJI_SAG_KARAKTER = {tofi, moni}` — kanal sahipleri; bu bilgi dosya adlarından TÜRETİLEMEZ, o yüzden bilerek kodda. (b) **A1 (videoyu çeken) adı ne olursa olsun SAĞDA.** İkincisi olmadan, kadrosunda ne Tofi ne Moni bulunan bir kullanıcıda (ParsMazi) hiçbir karakter listede olmaz, **hepsi sola düşer ve hepsi aynalanırdı** — videoyu çekenin kendi yüzü sol köşede ve TERS, sağ köşe hiç kullanılmaz. Panelde bunu düzeltecek ayar yok; kaynak dosya düzenlemek gerekirdi, ki ikinci kullanıcının yapamayacağı tek şey bu. Rol kuralı kadrodan bağımsız doğru, liste onun üzerine binen kullanıcı isteği.
  - ⚠ **DİKEY (Shorts) SEKANSTA TARAFLAR ÇAPRAZLANIYORDU.** Ölçek kare YÜKSEKLİĞİNE bağlı: 1080×1920'de emoji 1102 px, yani kareden GENİŞ; "sağ" formülü 0.481, "sol" 0.519 veriyor — sağdaki soldan solda kalıyor. `emojiKonum` artık iki tarafı 0.5'te kelepçeliyor (yatay videoda hiçbir şey değişmiyor). **Kelepçe yalnız "hangi taraf" bilgisini dürüst tutar, taşmayı ÇÖZMEZ — Shorts'ta emoji hiç ölçülmedi.**
  - ⚠ **TARAF/AYNA SAYIMI PLAN SATIRINDAN GERİ TÜRETİLMEZ** (`planSag[]` / `planAyna[]` paralel dizileri, seyreltmede planla BİRLİKTE süzülür). x alanına bakmak dikeyde ters okuyor, yol dizgisinde "/ayna/" aramak kırılgan, ham planı saymak da tavana takılan planda gerçekte konmayan emojileri raporluyordu.
  - **AYNALAMA PREMIERE'DE DEĞİL, DOSYADA** — `js/pngayna.js` (yeni): kütüphanesiz PNG çöz → satırları ters çevir → yeniden kodla. Kopyalar `<Emoji>\ayna\` altında önbelleklenir (ölçüldü: 1000px ~100 ms, 2000px ~280 ms; ikinci çağrı 1 ms). Kaynak resim yeniden kaydedilirse mtime karşılaştırmasıyla kendiliğinden tazelenir.
    **Neden Premiere'in "Horizontal Flip" efekti DEĞİL:** o yol ancak kullanıcının makinesinde, Premiere açıkken doğrulanabilirdi (efekt katalogda var mı, adı dile bağımlı mı, `_qeKlipBul` 150 klipte ne kadar sürer — hiçbiri ölçülmedi). Dosya yolu burada, **gerçek 35 PNG ile** sınandı: 35/35 başarılı, .NET System.Drawing ile bağımsız doğrulama 2000 örnek noktada 0 sapma (kendi çözücümle doğrulamak totoloji olurdu — bkz. preset bölümündeki aynı ders).
    **Bedava gelen iki şey:** `emoji.js tara()` alt klasörleri atladığı için "ayna" sahte bir karakter üretmiyor; `emojiKanalOzet` yol ÖNEKİ karşılaştırdığı için ayna kopyaları yine "bizim emoji" sayılıyor (yani "Emojileri Sil" onları da temizliyor). Ayna kararı plan satırındaki dosya YOLUNA gömülü, yani plan biçimi 8 alanlı kalıyor.
    - ⚠ **YAN CHUNK'LAR KORUNMAK ZORUNDA — ÖLÇÜLDÜ.** İlk sürüm yalnız IHDR/PLTE/tRNS/IDAT/IEND yazıyordu. Kullanıcının **35 emoji dosyasının HEPSİNDE `sRGB` ve `pHYs` var**: atılınca ayna kopyası renk uzayı etiketsiz ve DPI'sız kalıyor, yani Premiere'de sol emojiler sağdakilerden farklı renkte/ölçekte çıkabilir. Artık `iDOT` (Apple'ın IDAT satır indeksi — yeniden sıkıştırınca **geçersiz**) ve `eXIf` (içinde Orientation olabilir, çevirmeden sonra yanıltıcı) DIŞINDA her şey özgün sırasıyla taşınıyor. Doğrulandı: aynalanan dosyanın DPI'ı 72, özgünüyle aynı.
    - ⚠ **`AYNA_SURUM` + `<ayna>\.surum` — ÖNBELLEK KODA DA BAĞLI.** Tazelik önce yalnız kaynak PNG'nin mtime'ına bakıyordu; aynalama KODU düzelse bile (chunk düzeltmesi gibi) kaynak dosya değişmediği için eski, eksik kopyalar sonsuza kadar kullanılmaya devam ederdi — ikinci kullanıcıda hiç fark edilmezdi. Üretim mantığı değişince `AYNA_SURUM`'u ARTIR, bütün kopyalar bir kez yeniden üretilir (11 dosya, ~1 sn).
    - ⚠ **`host.jsx` `klasorler` kümesi bir üst klasörü de alıyor.** Plan artık İKİ klasöre yayılıyor (`<Emoji>\` ve `<Emoji>\ayna\`) ve o küme plan PARÇASI başına (40'lık dilimler) kuruluyor: bir devam parçasının 40 satırının tamamı sol taraf olursa küme yalnız ayna klasörünü içerir, önceki parçanın kök klasörden gelen emojilerini "yabancı klip" sayıp yerleştirmeyi ORTADA durdururdu. Bin haritası normalde bunu yakalar ama `createBin` başarısız olup PNG'ler proje köküne import edildiğinde harita boş kalır — kullanıcının projesinde bir kez gerçekten oldu (5 katman / 479 klip). Tek satırlık `ayna\` → üst klasör kuralı o ikinci emniyetin delinmesini önlüyor.
    ⚠ Aynalanamayan resim emojiyi DÜŞÜRMEZ (özgün resim konur) ama **kısmi başarı** sayılır ve sarı yazılır: ters bakan emoji ancak video izlenirken fark edilir, yeşil "başarılı" yalan olurdu. Sayım **dosya başına**, emoji başına değil — emoji sayılsaydı hem aynı dosya onlarca kez sayılır hem seyreltilen (tavan) emojiler de sayıya girerdi.
    ⚠ Aynalanan resimde **asimetrik detay** (yazı, tek taraflı aksesuar) ters görünür. Kullanıcı bunu bilerek kabul etti.
- ⚠ **"1 emoji çıkıyor" sorununun ÜÇ ayrı sebebi vardı, üçü de düzeltildi** (hangisinin tek başına ne kadar payı olduğu ayrıştırılmadı):
  1. **Cümlelerin %52'si yapay zekâdan SONRA eleniyordu.** Karakter eşleşmesi plan döngüsündeydi: eşleşmeyen kanalın cümleleri modele gidiyor, **parası ödeniyor**, sonra atılıyordu (ölçüldü: 465 cümlenin 240'ı; `PARCA=250` yüzünden 1 yerine 2 istek). Eşleştirme artık `emojiCumleleriTopla(tarama)` içinde, **kanal başına bir kez, AI'dan ÖNCE**.
  2. **A1'in isim kutusu YOKTU** — statik bir `<div>`'di ve adı `lsGet("kanalAd.0") || "sen"` diye okunuyordu; emoji klasöründe "sen" olmadığı için kullanıcının kendi 223 cümlesi (en çok konuşan kanal) sessizce eleniyordu. Artık gerçek input, **erken dönüşten ÖNCE çiziliyor** (A2+ boşsa da), canlı okunuyor (`a1Adi()`, `state.a1AdInput`) ve **oturuma yazılıyor** (`a1Ad`) — localStorage CEF önbelleğinde ve koruma listelerinin dışında.
  3. **İstem kendi içinde çelişiyordu:** "her 3-4 cümleden birini işaretle" + "emin değilsen İŞARETLEME". İkincisi birinciyi yiyordu. Artık somut **hedef sayı** gönderiliyor (`hedefOran`) ve "iki duygu arasında kaldıysan EN YAKINI seç" deniyor. **Buraya hacmi kısan yeni bir kural eklerken bunu hatırla.**
- **Sıklık panelden ayarlanır** (`#emojiSiklik`: Az 0.25 / Orta 0.40 / Bol 0.60). Ölçüldü (22 dk, 448 uygun cümle): **~112 / ~176 / ~236 emoji**. Kaynak dosya düzenlemeyi gerektirmesin diye seçici kondu; AI'nın gerçek işaretleme sayısı **durum satırına** yazılıyor ("448 cümlenin 180'ini işaretledi") — ayar kolu o sayı, log'a gömülü kalmamalı.
- **Eşleşmeyen ad → koşullu seçici satırı** (`#emojiEsleBox`). Anahtar **ASCII AD** (`emoji.esle.<ad>`), **kanal numarası ASLA** — numaranın anlamı kadroya göre değişiyor (AutoCut'taki `acCh → acCh2_` göçünün sebebi). "emoji yok" kalıcı bir seçenek (resmi gerçekten olmayan karakter için). **Kararı olmayan ad varken AI'ya istek GİTMEZ.** Adı boş A1 "sen"e değil **"A1"e** düşer, yani bu seçiciye gelir — "adını yaz" deyip kutuyu gizli bırakma çıkmazı yok (kanal listesi yalnız "Herkes" modunda görünür).
- ⚠ **"VİDEOYU MONİ ÇEKTİ AMA TOFİ'NİN EMOJİSİ ÇIKTI" — ÜÇ TAŞIYICI VARDI, ÜÇÜ DE KAPATILDI** (8 Ağustos 2026). Ortak kök: **A1'in kim olduğu videodan videoya taşınan GLOBAL bir localStorage değerinden okunuyordu ve panel hiçbir yerde "bu hâlâ geçerli mi" diye sormuyordu.** Kadro sabit değil — bir video Tofi'nin, öteki Moni'nin.
  1. **`emoji.esle.a1` kalıcı eşleşmesi.** A1'in isim kutusu boşken ad "A1"e düşüyor, anahtar `emoji.esle.a1` oluyordu. Kullanıcı bir kez "A1 → Tofi" dedikten sonra bekleme kapısı (`if (!e.karar)`) bir daha HİÇ tetiklenmiyor, panel sessizce Tofi'yi koyuyordu; tek iz katlanmış log'daki bir satırdı. **Artık kanal kodu (`/^a\d+$/`) için kayıtlı karar OKUNMUYOR** — `emojiYerTutucuAd()`. Kanal kodu bir KİMLİK değil; bu, "numarayla saklama" kuralının kaçırılmış üçüncü hâliydi.
  2. **Panel doğru cevabı ZATEN biliyordu ama bağ yoktu.** Senkron kartında "Videoyu kim çekiyor?" seçicisi (`#snkCeken`) var ve `snkEslestir` planın ilk satırını `{kanal:0, karakter: çeken}` diye kuruyor. Emoji ise adı `kanalAd.0`'dan okuyordu — iki kaynak arasında tek satır kod yoktu. **Artık `kanalAdlariniPlandanYaz(plan)` planı kanal isimlerine yazıyor** (localStorage + CANLI kutular) ve `a1Adi()` son çare olarak `snkCeken`'i okuyor (Kaynak Ses A1/A2 iken kanal listesi hiç çizilmiyor, yani `state.a1AdInput` null kalabiliyor).
  3. **Kalan belirsizlik için ONAY.** `emojiEkle`, yapay zekâya istek GİTMEDEN önce bütün eşlemeyi gösterip "bu videoyu X mi çekti?" diye soruyor. Emsal: `capBasildi_<sekans>` ve `cuesStale` onayları. Emoji para harcayıp 300 klip koyuyor — tek onay satırı, render'dan sonra fark edilen yanlış yüzden ucuz.
     - **Kapı A1'e DEĞİL EŞLEMENİN TAMAMINA bağlı.** Önce yalnız A1 kaydına bakıyordu ve A1'den hiç cümle gelmeyen durumlarda (adı "emoji yok" işaretli · A1 klipsiz · A1'in bütün cümleleri 5-kelime filtresine takıldı) **sessizce devre dışı** kalıyordu — üstelik tam da isimlerin bayat olduğu senaryoda.
     - **Hafıza OTURUMLUK (`_emojiEslemeOnay`), localStorage DEĞİL.** Anahtar sekans adına bağlıydı; Premiere'in varsayılan adları ("Video Sequence") projeler arası tekrar ettiği için **başka bir projenin onayı bu videonun sorusunu susturabiliyordu** — kapının önlemek için var olduğu hatanın ta kendisi. Panel kapanınca soru yeniden sorulur.
  4. ⚠ **KAYNAK SES = A2 DALINDA ARKADAŞLARIN SESİ SENİN KARAKTERİNE YAZILIYORDU** (pre-existing, aynı pakette düzeltildi). Tek kaynak dalı koşulsuz `ekle(a1Adi(), state.singleCues, true)` çağırıyordu; ama `state.track === "1"` iken cue'lar A2'nin (arkadaşların karışık Discord kanalı) sesidir. Sonuç: 300 cümlenin hepsi senin yüzünle, hepsi sağda, hiçbiri aynalanmadan. Daha kötüsü: kimlik kapısı "A1 (senin mikrofonun): Tofi → Tofi" gösterip "bu videoyu Tofi mi çekti?" diye soruyordu — cevap gerçekten "evet" olduğu için kullanıcı onaylıyor ve panel **yanlışı imzalıyordu**. Artık A2 kendi adıyla (`kanalAd.1`, yoksa "A2" yer tutucusu) ve `a1=false` ile ekleniyor.
  ⚠ Dördüncü bir taşıyıcı DAHA var ve kapatılmadı: `restoreSession` (app.js) `lsSet("kanalAd.0", o.a1Ad)` ile global anahtarı eziyor ve oturum dosyaları YALNIZ sekans adıyla adlandırılıyor (`oturum_<sekans>.json`) — Premiere'in varsayılan adları ("Video Sequence") projeler arası tekrar ettiği için başka bir projenin oturumu okunabilir. Yukarıdaki 3. madde bunu yakalar (onay sorusu çıkar), ama kökten çözüm oturum dosyasına proje adını da katmaktır.
- **SÜRE SABİT DEĞİL — emoji O CÜMLE BOYUNCA kalır** (kullanıcı isteği, 7 Ağustos 2026). Eskiden herkes `EMOJI_SURE = 1.6` sn kalıyordu ve tepki resmi konuşmanın ortasında kayboluyordu. Süre `VUR.cumleleriCikar`'ın verdiği `bit - bas`; `EMOJI_MIN_SURE` / `EMOJI_MAX_SURE = 10.0` yalnız **emniyet** (ölçüldü: cümleler 0.80-6.41 sn, ortanca 3.52 — tavana çarpan SAYILIP log'a yazılıyor).
- **EMOJİ EN AZ 5 KELİMELİK CÜMLEYE KONUR** (`EMOJI_MIN_KELIME = 5`, kullanıcı isteği 8 Ağustos 2026: "cümle 3 kelimelikse garip duruyo, 1 saniye gelmeden gidiyo"). Taban süre de 0.6 → **1.2** sn çıkarıldı (ikinci emniyet; ölçüldü: 5 kelime filtresinden sonra 1 sn altında cümle KALMIYOR, yani pratikte devreye girmiyor).
  - **ELEME YAPAY ZEKÂ ÇAĞRISINDAN ÖNCE** (`emojiCumleleriTopla`). Bu projede aynı hata bir kez yapıldı: eşleştirme plan döngüsündeydi ve cümlelerin %52'si modele gidip **parası ödendikten sonra** atılıyordu. Ayrıca hedef sayı (`hedefOran`) böylece gerçekten yerleştirilebilir cümleler üzerinden hesaplanıyor — "Bol emoji" sözünü tutuyor.
  - **ÖLÇÜLDÜ (kullanıcının gerçek oturumu, 172 cue / 26 cümle):** 5 kelime eşiği 26 cümlenin **1'ini** eliyor ("Aa!", 0.80 sn) — tam da şikâyet edilen tip. Emoji SAYISI hiç değişmiyor (15 → 15), çünkü asıl darboğaz `EMOJI_SECIM_PENCERE` (3 sn). Taban süreyi 0.6/1.2/2.0 yapmak da sayıyı değiştirmiyor. Yani bu iki ayarın maliyeti ölçülen veride **sıfır**. ⚠ "Cümle" burada Whisper SEGMENTİ (ortalama 3.77 sn, 26 cümlede ortanca 18 kelime) — cue değil; eşiği kelime cinsinden büyütmeden önce bunu hatırla.
  - Elenen sayı **durum satırına** yazılıyor ("… 12 kısa cümle baştan elendi"): emoji sayısı düşünce kullanıcı sebebini görmeli, Sıklık'ı boşuna artırmamalı (o kol bu elemeden SONRA çalışıyor).
- **Çakışma freni artık BİTİŞE bakıyor** (`EMOJI_GAP = 0.08`), "başlangıçlar arası sabit mesafe"ye değil — süre değişken olduğu için eski `EMOJI_ARA` ölçüsü anlamsızlaştı (6 sn'lik cümlenin emojisi 2 sn sonrakiyle çakışırdı). ⚠ **SIFIR YAPMA:** host her klip konar konmaz süresini yazıyor ve sonraki ancak ondan sonra konabiliyor; kare yuvarlaması yukarı giderse çakışan emoji ezilmez ama **sessizce atılır** ve her çalıştırma sarıya döner.
- **Parçalı yerleştirme (`EMOJI_PARCA = 40`) + `devamMi` bayrağı.** Tek `evalScript` boyunca Premiere donuyor; 150+ emoji tek çağrıda gitseydi kullanıcı "kilitlendi" sanırdı. Parça süresi log'a yazılıyor.
  - **v1.8.0 koruması ZAYIFLAMADI:** ilk parçada kanal **BOŞ olmak zorunda**; devam parçalarında "boş VEYA yalnız emoji" — tek yabancı klip varsa hiçbir şey yapılmıyor.
  - ⚠ `sonBitis` **guard bloğunda bir kez** tanımlanır. Döngüde ikinci bir `var sonBitis` olsaydı ES3 hata vermeden değeri ezer ve parçalar arası çakışma freni "yazılmış ama çalışmıyor" olurdu. Fren **geri okunan** bitişten kurulur, istenenden değil (Premiere kareye yuvarlıyor).
- **Panel tavanı 300, host tavanı 400.** Host'un eski tavanı **100'dü ve SERT DUVARDI**: aşılınca tek emoji koymadan `err:` dönüyor, ödenmiş API isteği ve GPU işi çöpe gidiyordu. Panel artık reddetmiyor, **eşit aralıkla seyreltiyor**.
- ⚠ **İKİNCİ BASIŞ ÇİFT KATMAN TUZAĞI (çözüldü) — GERÇEKTEN OLDU.** Panel her çalıştırmada "en üstteki BOŞ kanal"ı seçiyordu; ilk çalıştırmadan sonra o kanal dolu olduğu için ikinci basış **bir üst kanala ikinci bir tam set** koyuyordu. Kullanıcının projesinde **beş katman** birikti: `V6:235 · V7:125 · V8:77 · V9:29 · V10:13` klip. Artık `emojiKanallariJSON()` hangi kanalın emoji katmanı olduğunu ölçüyor, eski katmanların **hepsi** temizlenip **en alttaki** uygun katman yeniden kullanılıyor. **Hedef yine de `enUstDolu`'nun ÜSTÜNDE olmak zorunda** — o kontrol düşerse emoji görüntünün ARKASINA gider ve panel "başarılı" der. `enUstDolu` yalnız **yabancı** klipten hesaplanır, yoksa kendi katmanımız hedefi bir üste iterdi.
- ⚠ **EMOJİ Mİ DEĞİL Mİ KARARINI BİN VERMEZ, DOSYA YOLU VERİR.** Tanıma `"Yusufwrl Emoji"` bin'ine bakıyordu; ama `emojiYerlestir` içinde `createBin` başarısız olursa kod `bin = root`'a düşüyor ve PNG'ler proje **köküne** import ediliyor — o zaman bin araması boş dönüyor ve panel **kendi koyduğu** emojileri kullanıcının görüntüsü sanıyor. Kullanıcının projesinde tam olarak bu oldu: 479 emoji "yabancı klip" sayıldı, panel "boş kanal yok" dedi ve "Emojileri Sil" onları temizleyemedi. Artık `emojiKanallariJSON` her kanalın **tekil medya yollarını** döndürüyor, emoji/yabancı kararını **panel** veriyor (yol, emoji klasörünün altında mı) ve silinecek yollar host'a **dosyayla** geçiriliyor (evalScript string'ine Türkçe karakter gömmek kırılgan — emoji klasörü "Masaüstü" içeriyor). `emojiYerlestir`'in devam kontrolü de aynı sebeple plan yollarından **klasör** türetiyor.
- ✅ **ÖLÇÜLDÜ VE KAPANDI (kullanıcı, gerçek projede, 9 Ağustos 2026): Tofi (2000px) ile Dora (1000px) emojisi ekranda AYNI BOYUTTA.** Yani `olcekHesapla`'nın dosya başına ölçek hesabı doğru çalışıyor ve *Default Media Scaling* ayarı bu yolu bozmuyor. Bu bir zamanlar "ölçülmedi, Tofi yarı boyutta çıkabilir" diye açık duruyordu — **bir daha araştırma.**
- ⚠ **YENİ EMOJİ EKLEYİNCE `installer\emoji-esitle.ps1`'İ ÇALIŞTIR — HİÇBİR OTOMATİK AKIŞ ONU ÇAĞIRMIYOR.** Ne `publish-github.ps1`, ne `pack-panel.ps1`, ne `/surum-yayinla` skill'i. Yani **sürüm çıkarmak tek başına yeni emojileri pakete SOKMAZ**. Bu gerçekten oldu: kaynak klasörde 35 PNG varken pakette, git'te, panel.zip'te, kurulu panelde ve motor klasöründe 30 vardı (5 yeni Moni/Mimi resmi kimseye ulaşmamıştı). Doğrulama: paketteki `varsayilan\emoji` dosya sayısı kaynak klasörle aynı olmalı.
- ⚠ **AYNI ADLA DEĞİŞTİRİLEN RESİM ARTIK TAZELENİYOR** (`varsayilanlariKur`, 8 Ağustos 2026). Eski kural "hedefte dosya varsa hiç dokunma" idi; yeni AD eklemek için doğruydu ama **aynı adla düzeltilmiş bir resmi kimseye ulaştırmıyordu** — kullanıcı 8 Tofi resmini yeniden kaydetmişti (sol kenarı kesik sorunu) ve o düzeltme ne güncellemeyle ne yeniden kurulumla motor klasörüne geçiyordu, panel "zaten kurulu (30)" deyip geçiyordu. Artık panel **kendi kurduğu dosyaların kaydını tutuyor** (`<Emoji>\.panel-emoji.json`, ad→boyut): boyut farklı **ve** kaydımızdaki boyutla eşleşiyorsa (yani kurduğumuzdan beri değiştirilmemiş) paketteki hâliyle tazeleniyor; kullanıcı elle değiştirmişse KORUNUYOR. Tazelenen her ad log'a **adıyla** yazılıyor. Kayıt dosyası yoksa (bu sürümden önceki kurulumlar) dosyalar "bizim" sayılır — ama **ezmeden ÖNCE eskisi `<Emoji>\eski\` altına alınır**: o varsayım yalnız geliştiricinin makinesinde güvenli, ParsMazi'de emoji klasörü "kaynak klasör" gibi kullanılıyor olabilir (panel o yolu kendisi yazıp "Emoji klasörü" diye gösteriyor). Alt klasör olduğu için `tara()` onu görmez. ⚠ `emoji.js tara()` artık "ayna" ve "eski" alt klasörlerini ve nokta ile başlayan dosyaları **`atlanan` sayacına KATMIYOR**: o sayaç "bir PNG'yi yanlış adlandırdın" uyarısı için var, panelin kendi ürettikleriyle kalıcı olarak şişerse gerçek bir hatalı ad o gürültünün içinde kaybolur.
- **Emoji sayısının gerçek tavanı KODDA DEĞİL KLASÖRDE:** 5 duygu da olumsuz ağırlıklı (Ağlayan/Düşünceli/Korkmuş/Yalvaran/Şaşırmış). Minecraft/Roblox videosunun en sık tepkisi olan **gülme/mutluluk yok** (yalnız `Emoji\w` altında ve yalnız Tofi için). Dora/Mimi/Moni için birer "Mutlu" PNG eklemek, koddaki her ayardan daha çok emoji üretir — panel klasörü kendiliğinden okur.

## Preset kartı (host.jsx: `efektListesiJSON` · `efektUygula` · `animasyonUygula`)
Premiere'de seçili klip(ler)e panelden tek tıkla efekt/animasyon. Kullanıcının isteği: "Effects'ten Pop In'i sürüklemek yerine eklentiden tıklayayım." Kartta **iki ayrı yol** var:
- Ana ekranda **kendi bölümü** (`#viewPreset`, `data-view="preset"`, `.grad-e`). Altyazı'nın altında DEĞİL: timeline'da ne seçiliyse ona uygulanan genel bir araç.
- **KULLANICININ PRESET'LERİ ERİŞİLEMEZ — ölçüldü, soru KAPANDI (6 Ağustos 2026).** `getVideoEffectList()` **148 öğe** döndürdü ve hepsi Premiere'in yerleşik efekti (Shrink, Spin, Stroke, Vignette, Wiggle, Wonder Glow…); kullanıcının "Yusufwrl" bin'indeki 8 preset'in **hiçbiri** yok. `qe.project`'te yalnız dört katalog var (video/ses efekt, video/ses geçiş) ve preset hiçbirinde değil. Preset'i yeniden adlandırmak da değiştirmiyor. **Tek yol: her preset'i panel animasyonu olarak YENİDEN YAZMAK** (`PRESET_ANIM` tablosu, app.js ↔ host.jsx `animasyonUygula`).
- **KEYFRAME ZAMANI = KAYNAK MEDYA ZAMANI (`clip.inPoint + t`).** Sekans zamanı DEĞİL, klip başından sıfır da DEĞİL. Adobe belgeleri iki bağımsız yerde bunu söylüyor; resim/still ögelerde `inPoint` **3600 sn** olabiliyor, kırpılmış her klipte (AutoCut'tan geçen hepsi) sekans zamanından farklı. Bu taban denenmediği sürece keyframe'ler klip aralığının dışına düşüyor ve Premiere hepsini **sessizce klip sınırına kırpıyor** → hepsi tek keyframe'e çöküyor. 20 turluk hata avının kök sebebi buydu; `_klipKaynakBas` ile ele alınıyor ve `presetYaz` **üç adayı** (kaynak · sekans · klip) sırayla deniyor.
- **GERİ OKUMA TEK BAŞINA TOTOLOJİDİR.** `addKey` ve `getKeys` aynı zaman tabanını kullandığı için yanlış tabanla yazılan key aynı yanlış tabanla geri okunur ve "tuttu" sanılır. Doğrulamanın ayırt edici olması için okunan zamanın **klip aralığında** olup olmadığına bakmak ŞART (`_keyDene`'deki `arAlt/arUst`).
- **`addKey` hata FIRLATMADAN sessizce hiçbir şey yapabiliyor** (zaman klip dışındaysa) ya da key'i klip sınırına kırpıyor. Bu yüzden her yazma geri okunarak sayılır ve **başarısız denemeler temizlenir** (`_keySnapshot`/`_keyGeriAl`) — temizlik olmadan üst üste denemeler klipte tek bir çöp keyframe bırakıyordu.
- **`Time` nesnesi `ticks` ile kurulur**, `seconds` salt okunur olabiliyor ve sessizce 0 bırakıyor (1 sn = 254016000000 tick).
- **Yol 1 — Premiere'in yerleşik efektleri** (`getVideoEffectList` → `getVideoEffectByName` + QE `addVideoEffect`). Yalnız yerleşik efektler için çalışır (Crop, Gaussian Blur…) — preset için DEĞİL.
  **Liste KLASÖR BİLGİSİ VERMİYOR** — düz bir dizi, hangi öğenin hangi bin'den geldiği yok. Yani "yusufwrl bin'indekiler" diye otomatik süzmek **mümkün değil**. Bunun yerine kullanıcı "Düğmeleri seç" listesinden kendi preset'lerini **bir kez işaretliyor**, seçim `preset.secili` (JSON dizi, **adlarla**) anahtarında saklanıyor ve düğme olarak çıkıyor. Kullanıcı istediği için düğme; açılır liste denendi, reddedildi.
  Kayıtlı ad Premiere listesinde yoksa düğme **devre dışı + "(listede yok)"** yazar — sessizce çalışmayan düğme bırakmak yerine.
- **Yol 2 — panelin kendi yazdığı animasyon** (`animasyonUygula`, şimdilik `popin`): preset dosyasından bağımsız, her zaman çalışır.
- **Panel host'a AD DEĞİL SIRA NUMARASI gönderir.** Sebep: `evalScript` string literaline gömülen Türkçe karakter/tırnak kırılgan (proje geneli kural). Bunun bedeli: `efektListesiJSON` **sıralama YAPMAZ** ve boş adları bile dizide tutar — numara ham `getVideoEffectList()` sırasıyla birebir kalmak zorunda. Görünüm sıralaması panelde, kopya dizi üstünde yapılır.
- **Kayıtlı seçim ADLA saklanır** (`preset.son`), numarayla değil: Premiere'e yeni preset eklenince ham listedeki numaralar kayıyor ve kayıtlı numara sessizce BAŞKA bir efekti uygulardı.
- `_qeKlipBul` DOM seçimini QE klibine **aynı track + aynı başlangıç zamanı** ile eşler; **emin olamazsa `null` döner** — yanlış klibe efekt uygulamaktansa hata vermek yeğdir (yanlış klip sessizce bozulur, kullanıcı ancak videoyu izlerken fark eder).
- **Kullanıcının `.prfpset` PRESETLERİ OKUNAMIYOR — ölçüldü (26.3.0, `presetTani`).** Ne normal DOM'da ne `qe.project`'te preset metodu var: `getVideoEffectByName` · `getVideoEffectList` · `getAudioEffectByName` · `getAudioEffectList` VAR, preset karşılığı **yok**. Preset dosyasını uygulama yolu kapalı — arama.
- **Ama keyframe API'sinin TAMAMI açık.** `ComponentParam`: `addKey` · `setValueAtKey` · `setTimeVarying` · `setInterpolationTypeAtKey` · `removeKey` · `getKeys` · `areKeyframesSupported`. Ve her klipte **Motion + Opacity bileşenleri zaten var** — Pop In için efekt eklemeye gerek yok, doğrudan Scale/Opacity keyframe'i yazılıyor.
- Bu yüzden animasyon **preset dosyasından değil host.jsx'teki koddan** üretiliyor. Bedeli: kullanıcı kendi presetini kullanamıyor. Kazancı: preset dosyasına bağımlı değil, temiz kurulumda/başka makinede çalışır.
- `QE TrackItem`'da **`addVideoEffect` · `addAudioEffect` · `addTransition` · `removeEffects` var** — şimdilik gerekmiyor (Motion/Opacity hazır), ama yeni bir animasyon ek efekt isterse (bulanıklık, glow) giriş noktası bu.
- **`matchName` sadece Component'te var, ComponentParam'da YOK** (ölçüldü). Bileşen `matchName` ile aranıyor (dile bağımsız), parametre ise `displayName` ile — yani **Premiere'in dili değişirse parametre bulunamaz**. Bu yüzden `_paramAra` birden çok ad deniyor ve bulunamazsa klipteki bileşen listesini hata mesajına yazıyor (sessiz başarısızlık yerine).
- `_keyEkle` **dönüş değeri döndürür ve kontrol edilir**: keyframe zamanı bazı sürümlerde saniye, bazılarında `Time` nesnesi; ikisi de denenir. Sessizce başarısız olan bir keyframe "animasyon uygulandı" yalanına dönüşürdü.
- Yeni animasyon eklemek: `host.jsx`'e bir `_xxx(ti, sure)` fonksiyonu + `animasyonUygula` içine bir satır + `index.html`'e düğme + `app.js` `wireAnim` listesine bir satır.

### Preset ÖĞREN / TEKRARLA (host.jsx: `presetOkuJSON` · `presetYaz` — app.js: `presetOgren` · `presetUygulaAd`)
`.prfpset` dosyaları okunamadığı için (yukarı) TEK gerçek yol: kullanıcı preset'i bir klibe ELLE uygular → "Öğretme modu"nu açıp karta basar → panel klibin efekt yığınını OKUR (`presetOkuJSON`: components → properties → keyframe/statik) → `presetler.json`'a kaydeder → başka SEÇİLİ kliplere YAZAR (`presetYaz`). Premiere'in "Paste Attributes"i gibi ama düğmeye bağlı ve kalıcı. Çekirdek (keyframe zaman+değer) **5-lensli denetim + 2-yönlü doğrulamadan geçti (6 Ağustos 2026)** ve SAĞLAM. O denetimden çıkan kapsam kararları:
- **EĞRİ (ease/yavaşlama) ÖRNEKLEME İLE TAŞINIYOR — ÇALIŞIYOR, kullanıcı doğruladı (6 Ağustos 2026).** Bu maddeyi değiştirmeden önce tarihçeyi oku, iki çıkmaz sokak zaten denendi:
  1. **Interpolasyon tipini OKUMA: API YOK.** Adobe çalışanı forumda doğruladı — `setInterpolationTypeAtKey` (SET) var, `getInterpolationTypeAtKey` (GET) **YOK**. Kaynağın eğri tipi çekilemez.
  2. **"Hepsine Bezier yaz" DENENDİ, LİNEER'DEN KÖTÜ ÇIKTI.** `setInterpolationTypeAtKey(z, 5)` tutuyor ama tutamaklar (handle) **düz** geliyor → her keyframe'de hız **sıfıra** düşüyor, animasyon dur-kalk oluyor. Kullanıcının Effect Controls ölçümü: kaynak `Velocity: 22,6/second` (keyframe'in içinden akıyor), kopya `Velocity: 0,0/second`. Tutamak (velocity/influence) yazan API de yok, yani tip zorlamak çözüm DEĞİL. `_keyInterpUygula` duruyor ama **varsayılanı artık "dokunma" (-1)**; `KF_BEZIER` sabiti ileride tutamak API'si gelirse diye kaldı.
  3. **ÇÖZÜM — eğriyi tarif etme, ÖLÇ (`_egriOrnekle`, "baking").** `getValueAtTime(t)` iki keyframe **arasında** Premiere'in kendi interpolasyonunu uygulayıp değer döndürüyor (Adobe belgesi) — yani eğri okunamıyor ama **örneklenebiliyor**. Panel kaynak eğriyi **sekansın kare hızında** örnekliyor (`_sekansFps`, `ORNEK_MAX=300` tavan), **Douglas-Peucker** ile sadeleştiriyor (`_ornekSadelestir`; eşik değer aralığına GÖRE — mutlak taban 0..1 parametrelerde ease'i düzleştiriyordu) ve hedefe o noktaları yazıyor. Şekli anlatmak yerine şeklin kendisi taşınıyor: yavaşlama, hızlanma, overshoot **ve Position'ın uzamsal yolu** dahil. Kayıtta `p.s` = örnekler, `p.k` = özgün keyframe'ler; `_paramlariYaz` örnek varsa onu yazar, yoksa seyrek keyframe'e düşer (eski kayıtlar geriye uyumlu).
  - **Bedeli bilinçli:** hedefte keyframe SAYISI kaynaktakinden fazla (~10-30). Görsel sadakat > keyframe sayısı (kullanıcı isteği).
  - **Zaman biçimi DEĞERİ FARKLI İKİ keyframe'de doğrulanır** (`_ornekBicimOlc`). Tek noktada doğrulamak YETMİYOR: sayı bazı sürümlerde **tick** olarak yorumlanıyor ve ilk keyframe t≈0 ise tick yorumu da t=0'a denk gelip "tuttu" sanılıyordu → bütün örnekler aynı ana düşüp eğri **düzleşiyor**, panel yine de "başarılı" diyordu. Ayrıca **"düz çizgi freni"** var: kaynak değişiyorken örnekler sabit çıktıysa ölçüm yalan söylüyordur → örnekleme iptal, seyrek keyframe'e düş.
  - **Sayısal olmayan (enum/metin) parametre ÖRNEKLENMEZ** — `Number()` NaN verince sadeleştirme tüm basamakları yiyip iki uca indiriyordu; seyrek keyframe orada birebir doğru.
  - **`_yazVeSay` toleransı artık VERİDEN türetilir** (`minAralık*0.45`, taban 0.002), sabit 0.02 değil: örnekler birbirine yakın olduğu için sabit tolerans komşu key'le eşleşip sayımı yanıltıyor ve hayalet "N/M keyframe kondu" uyarısı üretiyordu. Eşleştirme de tek geçişli (O(N²) `_zamanSn` taraması 300 örnekte saniyeler sürüyordu). `_keyGeriAl` tavanı `ORNEK_MAX+64` (200'de durmak yüzlerce çöp keyframe bırakırdı).
  - **`po.k` zaman sırasına dizilir** — `getKeys` sıralı dönmeyebiliyor; çıpa/taban seçimi ve tek imleçli eşleştirme sıralı olmaya güveniyor.
- **STATİK-ONLY preset (animasyonsuz renk/blur/crop/look) artık DESTEKLENİYOR.** Eskiden `presetYaz` `kfSay==0` ise reddediyordu; artık iç-OLMAYAN bileşen statiği (veya blend mode) varsa uygular. Başarı ölçütü TÜRE bağlı: `animasyonlu ? sayac.kf>0 : sayac.st>0` — animasyonlu preset'te statik yazılıp key konmazsa HÂLÂ hata (eski anti-maskeleme korundu). Statik yazma artık geri okunur (`_statikTuttu`; dizi/renk için 0.02 tolerans — `setValue`'nun 8-bit nicemlemesi 0.001'i aşıp geçerli renk yazımını yanlış-negatif yapıyordu). `presetOkuJSON` uygulanabilir statik sayısını `stSay` olarak döndürür; öğren tarafı (`app.js`) buna güvenip statik-only preset'i öğrenir.
- **BLEND MODE kopyalama DENENDİ, GERİ ALINDI — ARAMA (gerçek testte BOZUK, 6 Ağustos 2026).** Opacity içsel sayıldığı için Blend Mode kopyalanmıyordu; istisna eklendi ama testte kaynak **"Normal" iken hedefte "Color"** oldu — Opacity'nin statik enum'unu `setValue` ile yazmak güvenilmez (`getValue`/`setValue` enum eşleşmesi TUTMUYOR, dile bağımlı). Geri alındı: Opacity yeniden TAM içsel, Blend Mode dahil statikleri yazılmıyor, kaynak neyse o kalır. Kullanıcının preset'leri (Pop In/Zoom) zaten Normal blend kullanıyor. Gerekirse enum değerleri reflect ile ÖLÇÜLÜP eklenmeli — **kör kopyalama arama**.
- **RENK keyframe MUTLAK yazılıyor.** Dizi-fark (diff) mantığı yalnız Position/Anchor Point'e uygulanır (`_spatialMi`); renk de `[r,g,b,a]` dizi döner ama diff yapılırsa hedefin mevcut rengine göre KAYIP bozulurdu. Diff'in amacı klibi kaynağın koordinatına sıçratmamaktı — renk için anlamsız.
- **fx AÇ/KAPA (`Component.enabled`) taşınıyor:** kaynakta kapalı efekt hedefte de kapalı gelir. Yalnız `enabled===false` yazılır (hiç `true` yapılmaz), eski `presetler.json` kayıtlarında alan yok → geriye uyumlu.
- **ÇIKIŞ (out) animasyonunda dizi taban İLK keyframe** (`_paramlariYaz`'a `capa` parametresi): giriş (`capa="bas"`) için son keyframe, çıkış (`capa="son"`) için ilk keyframe dinlenme (varış) noktasıdır. Yanlış taban Position/Anchor "out" preset'ini ters yerden başlatıyordu.
- **BİLİNEN SINIRLAR (belgelendi, ARAMA):** `_qeEfektEkle` yalnız `getVideoEffectByName`/`addVideoEffect` — **3. parti/plugin efektler ve TÜM ses efektleri eklenemez** ("katalogda yok" der) · Time Remapping keyframe güvenilirliği ölçülmedi · Motion STATİK ayarları (sabit zoom/anchor) bilerek kopyalanmıyor (klibi yerinden oynatmamak için) ama artık rapora "(klip kendi ayarı, atlandı)" diye yazılıyor (sessiz değil) · bezier tutamağının kendisi hâlâ yazılamaz — ama örnekleme sayesinde buna GEREK KALMADI (eğri nokta nokta taşınıyor).
- **İzlenecek (gerçek testte bak):** `_paramlariYaz`'da hedefin mevcut Position'ı okunamazsa dizi mutlak yazılıp klip kaynağın koordinatına sıçrayabilir (nadir, crash yerine tercih edildi).

#### Preset — ikinci tur iyileştirmeler (6 Ağustos 2026, 5-lensli araştırma + 3 doğrulama turu)
- **SES KLİBİ SÜZÜLÜR** (`_sesKlibiMi`, `mediaType === "Audio"`). Premiere'de **Linked Selection varsayılan AÇIK**: video klibine tıklayınca bağlı ses de seçime giriyor, panel onu ayrı klip sanıp **aynı görüntüye efekti İKİNCİ KEZ** ekliyordu (pop iki kat, sebebi görünmez). `presetOkuJSON` de artık `sec[0]` değil **ilk VIDEO** öğesini alır.
- **HIZ RAMPASI (Time Remapping) preset'e GİRMEZ** (`_hizRampasiMi`) — ne okunur ne yazılır. Hızlandırılmış bir klipten öğretilen preset uygulandığı her klibi ağır çekim/hızlı yapıyordu. Eski kayıtlar için yazma tarafında da süzülür.
- **STATİK yazımda tip kapısı:** yalnız **sayı ve boolean** yazılır, **metin/enum ATLANIR**. Metin yasağının sebebi: MOGRT/başlık klibinden öğrenilen preset hedef başlığın **yazısını siliyordu** (Source Text). ⚠ **Boolean ŞART** — `Uniform Scale`, `Use Composition's Shutter Angle`, `Crop > Zoom` boolean; dışarıda bırakmak "klip dikeyde eziliyor" hatasını geri getirir (bir doğrulama turunda bu regresyon gerçekten oluştu ve kullanıcının gerçek `presetler.json`'ı okunarak yakalandı).
- **ADI BOŞ parametreler indeksle eşlenir** (`po.ix`): `_paramBul` ada göre aradığı için iki adsız parametre (kullanıcının "Pop In RGB"indeki VR bileşeni) aynı özelliğe eşleşip birbirini eziyordu.
- **`_icselMi` TAM EŞLEŞME + matchName SONEK.** Eski `indexOf(...)===0` hali **Motion Tile / Motion Blur** gibi gerçek efektleri içsel sayıp statiklerini hiç yazmıyordu. matchName **sonek** ile eşlenir çünkü gerçek değer `AE.ADBE Motion` (ölçüldü) — tam eşitlik ölü koddu.
- **ESKİ KEYFRAME TEMİZLİĞİ** (`_yabanciKeyTemizle`, yalnız başarılı yazımdan SONRA): "uyguladım, beğenmedim, başkasını denedim" akışında iki animasyonun key'leri iç içe kalıyordu. Yalnız **yazdığımız aralıkta** ve hedef zamanlarımıza denk gelmeyen key'ler silinir; **aralık dışına dokunulmaz**.
- **DİNLENME DEĞERİ** (`_dinlenmeDegeri`): fark tabanı artık `getValue()` ile değil keyframe'lerden okunur. `getValue()` parametre zaten animasyonluysa **oynatma kafasının** olduğu andaki değeri veriyor — aynı preset ikinci kez uygulanınca sonuç kafanın yerine göre değişiyor ve klip her denemede kayıyordu.
- **KISA KLİBE SIĞDIRMA** (`_yiginSure` + `_yiginOlcekle`): AutoCut klipleri 0,2-0,5 sn; 1 sn'lik preset'in yarısı klip dışına düşüp Premiere tarafından sınıra yığılıyordu. Yalnız **küçültme** yönünde. ⚠ `_yiginSure` **yayılım (mx-mn) DEĞİL, çıpadan uzanım** döndürür (`capa="son"` → `-mn`); yayılım ölçüsü çıpadan uzakta başlayan preset'lerde sığdırmayı hiç tetiklemiyordu. Yığın paylaşıldığı için ölçeklenecekse **kopyası** alınır (`vk`), yoksa sonraki klipler de küçülürdü.
- **OYNATMA KAFASINA UYGULAMA** (`presetYaz(jsonYol, "1")`, sağ tık menüsü): gameplay kaydı tek parça olduğu için vurgu preset'leri ancak böyle kullanılabiliyor. Kafa zamanını **host kendisi okur** (`getPlayerPosition`), panelden sayı geçirilmez. Kafa klibin dışındaysa klip **atlanır** (sessizce klip başına düşmez). ⚠ **`capa` kafa modunda EZİLMEZ** — bir turda `"bas"` diye ezilmişti ve çıkış preset'lerinde spatial tabanı ters seçip klibi sıçratıyor, D10 kapısını da kapatıyordu; `capa` zamansal yerleşim için değil **"dinlenme noktası hangi keyframe"** için kullanılıyor (zamanı `capaOfs` hallediyor).
- **ORANSAL ÖLÇEK/OPAKLIK (D10):** preset'in varışı **varsayılan** ise (Scale/Opacity 100, Rotation 0) hedefin kendi duruşuna oranlanır — %35'e küçültülmüş overlay animasyon bitince **tam ekrana fırlıyordu**. Varış 100 DEĞİLSE (bilerek %50'de biten look) dokunulmaz; hedef zaten 100 ise formül birebir aynı sonucu verir.
- **`_qeKlipBul` iki geçiş, tavan 20000** (eskiden 500): 20 dakikalık AutoCut'lu videoda **ikinci yarıdaki klipler** "QE karşılığı bulunamadı" veriyordu ("başında çalışıyor, sonunda çalışmıyor"). Hızlı geçiş zaman sırasını kullanıp erken kırar; ad yedeği tam tarama yapar.
- **DÜRÜST SONUÇ MESAJI:** `ok:3/20 klibe uygulandı — 17 klipte OLMADI`. Kısmi başarı, atlanan klip ve "öğretilmiş kayıt YOK" durumları panelde **sarı** gösterilir (`sonucGoster`) — 20 klipten 1'i tutunca yeşil "uygulandı" demek render'dan sonra fark edilen en pahalı hataydı. Kart "öğretilmiş" görünümü **yalnız gerçek kayda** bağlı (yedek yollar dolu göstermiyordu).
- **PRESETLER.JSON ATOMİK YAZILIR + YEDEKLİ.** Sıra: `.tmp` yaz → eski dosyayı `.bak.yeni`'ye al → `.tmp`'yi ana dosya yap → `.bak.yeni`'yi `.bak` yap. Hata olursa eski dosya **geri konur**. Okuma sırası: ana → `.bak` → `.bak.yeni` → `.tmp`, kurtarma olursa kullanıcıya **söylenir** (`_presetKurtarildi`, `wirePreset`'te `presetYiginlar()` zorla çağrılır yoksa kart listesi boşken uyarı hiç tetiklenmiyordu). **`presetler.bak.json` beş korunan-dosya listesinde.**
- **ÖĞRETME MODU KUTUSU KALDIRILDI** → sol tık HEP uygular, **sağ tık** = öğret / kafaya uygula / tersini oluştur / yeniden adlandır / kaldır. Kartlar iki modda da birebir aynı görünüyordu ve yanlış modun bedeli asimetrikti (yanlışlıkla uygulamak zararsız, yanlışlıkla öğrenmek saatlerce emeği siliyordu). Üzerine öğretme ve kaldırma artık **onay sorar**.
- **PRESET'İN TERSİNİ ÜRET** (`presetTersiUret`, app.js — host'a dokunmaz): `t → -t`, listeleri yeniden sırala, `capa`'yı çevir, **ayrı ad** ("… (çıkış)") ile kaydet. Pop In öğret, Pop Out'u panel üretsin. Orijinalin üstüne asla yazılmaz.
- ⚠ **`_yazVeSay`'da `hedefler[]` ile `sonuc[]` indeksleri HİZALI kalmak ZORUNDA** (atlanan dallarda da `push(null)`). Bir turda hizasızlık girdi ve `v:null` içeren **eski kayıtlarda** doğrulama tek imleci gerçek key'lerin ötesine itip sayımı 0'a düşürüyor, `_keyGeriAl` de **doğru yazılmış** keyframe'leri siliyordu.

## Hazır içerik: `varsayilan\` (arkadaşın makinesinde de dolu gelsin)
Panel paketine giren **hazır içerik** klasörü. `js/app.js` → `varsayilanlariKur()` açılışta (wirePreset içinde, kartlar çizilmeden ÖNCE) kurar.
- `varsayilan/presetler.json` — öğretilmiş preset yığınları. **Yalnız kullanıcının hiç `presetler.json`'ı yoksa** kopyalanır; varsa dokunulmaz. Kurulunca preset adları `preset.secili`ye de eklenir, yoksa kayıt var ama kart yok olurdu.
  - ⚠ **"KULLANICININ KENDİ DOSYASI VARSA HİÇ ÇALIŞMA" KURALI PAKET GÜNCELLEMELERİNİ YUTUYORDU — AYNI TUZAK İKİ YERDE.** Blok `presetler.json` varsa tümden atlanıyor, ama o blok **aynı zamanda `preset.secili` birleştirmesini de yapıyordu**: pakete SONRADAN eklenen preset'ler mevcut kullanıcıya hiç ulaşmıyordu. Gerçekten oldu (kullanıcı ekran görüntüsü, 8 Ağustos 2026): `presetler.json`'da "Emoji Sağ Taraf" ve "Camera Shake 1" öğretilmiş duruyordu ama **kartları yoktu**, yani erişilemez kayıtlardı. Emoji PNG'lerindeki "üzerine yazma" tuzağının birebir aynısı — **yeni bir hazır içerik türü eklerken bu soruyu sor: mevcut kullanıcıya nasıl ulaşacak?**
  - **Çözüm pakete değil VERİYE bakıyor:** `presetSeciliTazele()` (wirePreset içinde, kartlar çizilmeden ÖNCE) öğretilmiş her yığının kartı olduğundan emin oluyor. Paket büyüdükçe kendiliğinden doğru kalıyor. "Bilerek kaldırdığım kart geri geliyor" durumu yok: `presetKaldir` adı ve yığını BİRLİKTE siliyor.
  - `PRESET_VARSAYILAN` hâlâ bir TAHMİN listesi ve içinde öğretilmemiş adlar var ("Cinematic Border Out", "Zoom Out Center"): kartları çıkıyor ama devre dışı görünüyorlar. Silinmiyorlar çünkü kullanıcı onları sonradan öğretmek isteyebilir — ama ParsMazi'nin "preset'ler eski gelmiş" şikâyetinin kaynağı buydu.
- `varsayilan/stiller/*.prtextstyle` + `varsayilan/stiller.json` — Premiere **Track Style** dosyaları. Kullanıcının Belgeler klasöründeki `Adobe\Common\Assets\Text Styles`'a, **o adda dosya YOKSA** kopyalanır.
  - **Dosya adları pakette ASCII** (`stil01.prtextstyle`) ve gerçek adlar `stiller.json`'dan okunur. Sebep: zip'e Türkçe karakterli dosya adı koymak (`Kırmızı Text.prtextstyle`) bozulma riski taşıyor; `Compress-Archive`/açma zincirinde ad mahvolursa stil sessizce gitmez.
  - **Belgeler klasörü DİLE ve OneDrive'a göre değişiyor** (`Documents` · `Belgeler` · `OneDrive\Documents` · `OneDrive\Belgeler`). `stilKlasoruBul()` adayları tarar ve **içinde `Adobe\Common` OLANI** tercih eder — Premiere'in gerçekten kullandığı yol odur.
- ⚠ **`.gitignore` kuralları `/` ile KÖKE SABİTLİ olmak zorunda.** Başta `/` olmadan `presetler.json` kuralı **her klasörde** geçerli olur ve `varsayilan/presetler.json` sessizce repoya girmez → arkadaşın panelinde preset'ler boş gelir. Bu tuzağa bir kez düşüldü, `git check-ignore -v` ile yakalandı.
- `varsayilan` **`$PanelInclude`'a eklendi** (`installer/panel-files.ps1`) — yoksa pakete hiç girmez.

## Komutlar (PowerShell)
- **Değişiklikleri panele gönder (her seferinde gerekir): `powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy-dev.ps1`** — kurulu panel junction değil KOPYA olduğu için repo değişiklikleri kendiliğinden yansımaz. Kullanıcıya özel dosyaları korur.
- İlk kurulum (PlayerDebugMode): `powershell -NoProfile -ExecutionPolicy Bypass -File .\install-dev.ps1` (veya `KUR.bat`). ⚠ Bu betik junction kurar — bu yolda CEF'i bozuyor, junction oluşursa kaldırıp `deploy-dev.ps1` kullan.
- Kurulumdan sonra Premiere'i **tamamen kapat-aç** → Window (Pencere) > Extensions > Yusufwrl Premiere.
- **Yeni emoji PNG'si eklendiyse ÖNCE: `powershell -NoProfile -ExecutionPolicy Bypass -File .\installer\emoji-esitle.ps1`** — pakete emojiyi SADECE bu betik koyar, hiçbir yayın akışı onu çağırmıyor. Çıktıdaki "karakterler / duygular" satırlarını oku; "UYARI kalıba uymuyor" varsa dosya adını düzelt.
- Otomatik-güncelleme zip'i: `powershell -NoProfile -ExecutionPolicy Bypass -File .\installer\pack-panel.ps1`.
- GitHub sürümü yayınla: `powershell -NoProfile -ExecutionPolicy Bypass -File .\installer\publish-github.ps1` (commit + push + release; `gh` kurulu ve girişli).
- Kurulum .exe'si (Inno Setup gerekir): `& "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe" .\installer\installer.iss` (ISCC PATH'te değildir).

## Tuzaklar
- **Altyazı artık video kanalı KULLANMIYOR.** v1.8.0 öncesinde her altyazı bir MOGRT klibiydi ve `idx = (videoKanalSayısı - 1) - lane` hesabı yanlış kanala denk gelince kullanıcının görüntüsünü siliyordu; katman bütçesi, `_stilBeyazListe` beyaz listesi ve `#STILLER|` başlığı bu yüzden vardı. Caption track'e geçişle bu mekanizmaların **tamamı kaldırıldı**. Yeni kod video kanalına hiç dokunmuyor — buraya bir daha lane/katman mantığı eklemeden önce bu tarihi hatırla.
- **Derleme yok** — HTML/JS/JSX düz dosyalar. Dosya değişince paneli kapat-aç (ya da Premiere'i yeniden başlat) → yeniden yüklenir.
- Panel JS'i CEP'in **kendi eski Node'unda** çalışır, sistem Node v24'te değil. ES5 tarzı kal; sadece `require()` ile built-in modüller.
- Sürüm çıkarken `version.json` **ve** `CSXS/manifest.xml` (ExtensionBundleVersion) birlikte artır. `installer.iss` AppVersion ayrı ve ayrıca güncellenir.
- **ffmpeg PATH'e gerekmez** — pipeline.js motorun içindeki `ffmpeg.exe`'yi tam yolla çağırır (Faster-Whisper-XXL kendi ffmpeg'ini getirir). **ffprobe YOK** — akış bilgisi `ffmpeg -i` çıktısından ayrıştırılır (`_probeAudioCount`).
- **Track ≠ ses akışı:** A1/A2/A3 track'i, medya dosyasının 1./2./3. ses akışına eşlenir — bu yalnızca OBS çoklu-kanal kaydında (tek dosya, çok akış) geçerli. Tek akışlı kayıtta ffmpeg `filter_complex` içindeki `[i:a:1]` "matches no streams" ile ÇÖKER (`-map`'in aksine `?` ile opsiyonel yapılamaz). `buildTimelineAudio` akış sayısını ölçüp olmayan akışı istemez.
- `config.json` `%ENGINE%` / `~` token'ları kullanır; makineye özel yol **gömme** (auto-updater config.json'ı ezer). Program yolları (motor/ffmpeg) kullanıcı ayarı DEĞİLDİR — `updater.js` `configBirlestir()` onları yeni sürümden zorla alır, yoksa güncelleme sonrası eski yol kalıp motor bulunamaz.
- **Kullanıcı dosyaları:** `engine-root.txt`, `diarize-device.txt`, `sozluk.json`, `kisiler.json`, `assemblyai-key.txt`, `anthropic-key.txt`, `presetler.json`.
  ⚠ `presetler.json` = öğretilmiş preset yığınları. **localStorage'a YAZILMAZ** — kullanıcı her preset için Premiere'de elle bir sürükleme yapıyor, veri pahalı ve başka kaynağı yok; localStorage CEF önbelleğinde durur (panel klasöründe değil) ve koruma listelerinin dışında kalır. Liste artık **tek kaynakta**: `installer/panel-files.ps1` → `$PanelUserFiles` (`pack-panel.ps1` ve `deploy-dev.ps1` bunu okur). Ayrıca **elle tutulan üç kopya** var: `.gitignore`, `installer/installer.iss` `Excludes`, `installer/kur.ps1` `$koru`, ve `js/updater.js` `copyDir` skip listesi. Yeni bir kullanıcı dosyası eklerken `panel-files.ps1` + bu üçü/dördü güncellenir.
  ⚠ `config.json` bu listeye **AİT DEĞİL** — `installer.iss` `Excludes`'a eklenirse temiz kurulumda panel hiç açılmaz.
- Debug: panel açıkken Chrome/Edge'de `http://localhost:8088` → DevTools (panel HTML/JS). `host.jsx` için VS Code "ExtendScript Debugger".
- **Premiere 2026 (UXP):** ~~CEP panelleri 2026 sürümünde yüklenmeyebilir~~ — **bu uyarı ölçümle çürüdü: Premiere 26.3.0'da CEP paneli sorunsuz çalışıyor** (6 Ağustos 2026, kullanıcının makinesi). Uzun vadede UXP'ye taşıma yine de gündemde, ama "panel görünmüyor" sorununun sebebi olarak 2026'yı ARAMA — önce kurulum yolunu ve PlayerDebugMode'u kontrol et. UXP'nin tek somut cazibesi şu an caption API'si olabilir (CEP'te stil atama yolu kapalı, bkz. yukarıdaki üç ölçüm); orada da var mı ÖLÇÜLMEDİ, taşımayı bu varsayıma dayandırma.
- **Kurulu panel junction DEĞİL, KOPYA:** `%APPDATA%\Adobe\CEP\extensions\com.yusufwrl.premierepanel` ayrı bir klasör (OneDrive + Türkçe karakterli yol CEF'i bozduğu için ASCII yola kopyalanmış). Repo'daki değişiklikler oraya **kopyalanmadan** panele yansımaz — `install-dev.ps1` ya da elle `Copy-Item`. Sürüm numarası güncel görünüp kodun eski olması bu yüzden olur.
- **ExtendScript'e metin geçerken dosya kullan:** `evalScript` string literaline gömülen Türkçe karakter/tırnak/ters bölü kırılgan. Altyazı yerleştirme ve metin düzeltme metni `work` klasöründeki geçici dosyadan okur (`_readFileUTF8`).
- **Motor `--initial_prompt` varsayılanı `auto`** (hazır Türkçe preset). Özel isim ipucu için onu ezme; `--hotwords` ayrı kanaldır ve preset'i bozmaz. `--reprompt` varsayılanı `True` olduğu için ipucu tüm video boyunca taşınır.
